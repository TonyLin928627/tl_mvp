<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.spaceconnect.visitor">


    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tl_mvp">
        <activity android:name=".HomeActivity">

        </activity>

        <activity android:name=".screens.splash.SplashActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.login.LoginActivity" />
        <activity android:name=".screens.selectLocation.SelectLocationActivity" />
        <activity android:name=".screens.main.MainActivity" />
    </application>

</manifest>